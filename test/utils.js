import { expect } from 'chai';
import { getIntervals } from '../src/utils';

describe('utils', function () {
    describe('getIntervals', () => {
        it('should fail', () => {
            expect(1).to.equal(1);
        });
    });
});
